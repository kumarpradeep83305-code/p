{
  "name": "screener-app",
  "version": "1.0.0",
  "private": true,
  "description": "Screener Android app",
  "scripts": {
    "build:android": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/core": "latest"
  },
  "devDependencies": {
    "@capacitor/assets": "latest",
    "@capacitor/cli": "latest"
  }
}
